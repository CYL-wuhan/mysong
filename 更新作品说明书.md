# 更新作品说明书（陈熠乐 · 创作集）

> 用途：以后要上传新的音乐 / 绘画、改个人介绍，照着这份做就行。
> 前提：网站已按 `README.md` 第四节部署到 Netlify（连接 Git 仓库）并开启了 Identity。
> 本地 `http://localhost:8080` 只是你自己预览，外网看不到；外网地址是部署后得到的
> `https://你的站点.netlify.app` 或你绑定的域名。

---

## 方式一：网页后台上传（★ 推荐，零代码）

1. 打开后台：浏览器访问 `https://你的站点.netlify.app/admin`
   （用自己的域名就是 `https://你的域名/admin`）。
2. 登录：点「Continue with Netlify Identity」，去邮箱点魔法链接登录。
3. 在左侧集合「站点内容」里，看到三类：
   - **个人介绍**：改姓名、头衔、标语、自我介绍、头像、联系方式。
   - **音乐作品**：点「新增」，填曲名 / 日期 / 配文，用「封面图」「音频文件」两个字段直接上传图片和 mp3。
   - **绘画作品**：点「新增」，用「图片」字段上传画作，填作品名 / 日期 / 材质 / 配文。
   - **游戏机制 · 兴趣社介绍**：填兴趣社名称、社徽、标语、介绍、招新联系方式。
   - **游戏机制作品**：点「新增」，填作品名 / 日期 / 类型（桌游·解谜等）/ 配图 / 机制说明，可选填玩法或试玩外链。
4. 改完点右上角 **Publish**（发布），Netlify 自动重新构建，约 1–2 分钟上线。

> 上传的图片/音频会自动存到仓库的 `assets/uploads/` 目录，无需你手动管文件。

---

## 方式二：手动改文件（备用 / 本地调试）

适合想直接改数据文件、或用 Git 客户端提交的情况。

### 上传一首音乐
1. 把 mp3 放进 `assets/audio/`，例如 `assets/audio/night-train.mp3`。
2. 打开 `content.json`，在 `music` 数组里加一条（或直接在后台更省事）：
   ```json
   {
     "title": "夜行列车",
     "date": "2026-08",
     "cover": "assets/img/painting-1.svg",
     "src": "assets/audio/night-train.mp3",
     "description": "写于一次深夜的归途……"
   }
   ```
3. 保存 → 推送到仓库 → 网站自动更新，出现带播放器的卡片。

### 上传一幅画
1. 把图片（jpg/png/webp）放进 `assets/img/`，例如 `assets/img/sunrise.jpg`。
2. 在 `paintings` 数组里加一条：
   ```json
   {
     "title": "清晨的窗台",
     "date": "2026-07",
     "img": "assets/img/sunrise.jpg",
     "medium": "水彩 / 21×30cm",
     "description": "早起拉开窗帘那一瞬的光……"
   }
   ```

### 加一个游戏机制作品
把配图放进 `assets/img/`，在 `gameMechanics` 数组加一条：
```json
{
  "title": "连锁反应",
  "date": "2026-07",
  "type": "桌游 / 解谜",
  "cover": "assets/img/chain-reaction.jpg",
  "link": "",
  "description": "一套以触发—连锁为核心的卡牌机制……"
}
```

### 改兴趣社介绍
改 `content.json` 里的 `club` 对象：`name`、`avatar`、`tagline`、`bio`、`contacts`。

### 改个人介绍
改 `content.json` 里的 `profile` 对象：`name`、`title`、`tagline`、`bio`、`avatar`、`contacts`。

> 注意：线上内容以 `content.json` 为准（后台也是改它）。本地双击 `index.html` 看版式时用 `content.js` 兜底，仅用于预览。

---

## 容量与格式建议（避免传不动 / 加载慢）

| 类型 | 推荐格式 | 单文件大小 | 数量建议 |
|---|---|---|---|
| 图片 | webp / jpg | 300KB–1MB，最长边 ≤ 1600px | 几百张无压力 |
| 音频 | mp3 | 128–192kbps，单首 5–7MB | 几十首无压力 |
| 头像 | 正方形 jpg/png | ≤ 500KB | 1 张 |

- 免费额度参考（Netlify）：月带宽 ~100GB、仓库建议 < 1GB、单文件 > 100MB 会被拒。
- 想传**上百首歌 / 大量高清图**：把大音频放到外部存储（网易云 / 网盘 / 对象存储），后台只填外链；或联系我接 Git LFS。

---

## 常见问题

- **改了内容没更新？** 静态站有缓存，先强制刷新（Ctrl/Cmd+Shift+R）；部署本身有 1–2 分钟延迟，去平台看 Deploy 是否 Success。
- **图片/样式不显示？** 全部用相对路径，只要 `index.html` 和 `assets` 文件夹同级就不会错。
- **音频没声音？** 确认 `src` 路径对、文件确实在；浏览器需用户点击播放（自动播放被禁是正常策略）。
- **留言板加载不出来？** 需先在 GitHub 仓库开启 Discussions + 安装 Giscus 应用，并把 `assets/js/main.js` 顶部 `GISCUS` 的 repo/repoId/categoryId 填好（详见 `README.md` 第七节）。
- **页脚浏览量显示 0？** 本地预览正常显示 0；部署到公网后不蒜子才开始计数。

---

## 一句话流程（日常最常用）

打开 `/admin` → 选「音乐作品」「绘画作品」或「游戏机制作品」→ 点新增、填信息、传文件 → 右上角 **Publish** → 等 1–2 分钟 → 完成。
